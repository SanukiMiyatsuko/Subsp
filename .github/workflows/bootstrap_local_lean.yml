name: Bootstrap Local Lean

on:
  push:
    branches:
      - constructive-by-cases

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1
        with:
          auto-config: false
          build: false
          test: false
          lint: false
      - name: Package Lean 4.34.0 and source
        shell: bash
        run: |
          set -euo pipefail
          LEAN_BIN="$(elan which lean)"
          TOOLCHAIN="$(dirname "$(dirname "$LEAN_BIN")")"
          mkdir -p bootstrap/lean/bin bootstrap/lean/lib
          cp "$TOOLCHAIN/bin/lean" bootstrap/lean/bin/
          cp "$TOOLCHAIN/bin/lake" bootstrap/lean/bin/
          cp -a "$TOOLCHAIN/lib/lean" bootstrap/lean/lib/
          tar --zstd -cf bootstrap/lean-minimal.tar.zst -C bootstrap lean
          split -b 300M bootstrap/lean-minimal.tar.zst bootstrap/lean-part-
          tar --zstd -cf bootstrap/subsp-src.tar.zst --exclude=.git --exclude=bootstrap .
      - uses: actions/upload-artifact@v4
        with:
          name: local-lean-part-aa
          path: bootstrap/lean-part-aa
          compression-level: 0
      - uses: actions/upload-artifact@v4
        with:
          name: local-lean-part-ab
          path: bootstrap/lean-part-ab
          compression-level: 0
      - uses: actions/upload-artifact@v4
        with:
          name: local-lean-part-ac
          path: bootstrap/lean-part-ac
          compression-level: 0
      - uses: actions/upload-artifact@v4
        with:
          name: local-subsp-source
          path: bootstrap/subsp-src.tar.zst
          compression-level: 0
